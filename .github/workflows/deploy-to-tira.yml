name: Publish to TIRA

on:
  workflow_dispatch:
    inputs:
      approach:
        description: "The retrieval engine to publish, is contained in the step-03-retrieval-approaches directory"
        required: true
        default: ""

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    runs-on: ${{ matrix.runner.os }}  
    timeout-minutes: 45
    strategy:
      fail-fast: true
      matrix:
        runner:
          - os: ubuntu-latest
          - os: ubuntu-24.04-arm
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Set up Dependencies
        uses: actions/setup-python@v6
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Install TIRA Client
        run: |
          pip3 install 'git+https://github.com/tira-io/tira.git@rag4reports#subdirectory=python-client'

      - name: Publish to TIRA
        run: |
          tira-cli login --token ${{ secrets.TIRA_CLIENT_TOKEN }}
          tira-cli code-submission \
              --path step-03-retrieval-approaches/${{ inputs.approach }} \
              --task lsr-benchmark \
              --tira-vm-id reneuir-baselines \
              --dataset tiny-example-20251002_0-training \
              --command '/index-and-retrieve.py --dataset $inputDataset --embedding $embeddings --output $outputDir' \
              --mount-directory '$embeddings=lsr-benchmark/lightning-ir/naver-splade-v3-doc' \
              --external-docker-registry ghcr.io/reneuir/lsr-benchmark/${{ inputs.approach }} \
              --platform host

